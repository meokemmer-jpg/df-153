# DF-153 PVG-Cross-Entity-Revenue [CRUX-MK]
# Welle: 25 Domain: K_0

df_id: DF-153
df_name: pvg-cross-entity-revenue
welle: 25
domain: K_0
status: SKELETON-CONDITIONAL
crux-mk: true

mission: |
  Cross-Entity-Revenue-Aggregation fuer Place Value Group.
  HeyLou + 9dots + LexVance aggregiert. NIEMALS Inter-Entity-Transfer.

k11_cascade_isolation: hard
failure_blast_radius: 0
dependency_dlq_strategy: "Append-only Revenue-Log"

k12_distillation_resistenz:
  output_feeds_into_training: false
  provenance_required_in_output: true
  non_llm_validation_layer: true

k13_independent_ground_truth:
  external_anchor_type: filesystem
  external_anchor_validation_frequency: per_run
  pre_action_domain_check: true
  pre_action_backup_check: true
  pre_action_blast_radius_check: true

k14_human_override_decay:
  override_complexity: single_command
  override_documentation_freshness_days: 30
  martin_review_cadence: weekly
  bus_factor_handover_drilled: false

k15_entropy_budget:
  entropy_added_loc_estimate: 200
  entropy_added_concepts: [Cross-Entity-Aggregation, Revenue-Tracking, Per-Entity-PnL]
  entropy_justified_by_rho:
    rho_low_eur_per_year: 8000
    rho_high_eur_per_year: 40000
    rho_confidence: PROVISIONAL

k16_concurrent_spawn_protection: both
lock_dir: /tmp/df-153.lock
lock_stale_age_h: 6
engine_pgrep_check: true
lock_release_on: EXIT_INT_TERM

lose_coupling:
  LC1_graceful_degradation:
    degradation_modes: [full, degraded_anchor, standalone]
    degradation_test_required: true
  LC2_direct_mode:
    direct_mode_available: true
    direct_mode_capability: 0.85
    direct_mode_trigger: "DF_153_REAL_API_ENABLED=false"
  LC3_circuit_breaker:
    circuit_breaker_timeout_s: 30
    circuit_breaker_open_threshold: 3
    circuit_breaker_half_open_test_interval_s: 300
  LC4_failure_isolation:
    state_externalization: true
    idempotent_operations: true
    dependency_dlq_separate: false
  LC5_health_check:
    health_check_dependencies: []
    health_check_degraded_score: 0.7
    health_check_endpoint_or_file: branch-hub/state/df-153-health.json

default_action:
  description: "Mock-Mode: Cross-Entity-Revenue-Report, KEIN Inter-Entity-Transfer"
  rho_per_run_eur: 50
  block_gates:
    - gate_id: K13_PAV
      block_reason: "K_0 Anker-Check"
      false_block_cost_eur: 5
      legitimate_block_condition: "Env unverifiziert"
  graceful_degradation_path: "Mock-Fallback"

activation_gates:
  DF_153_REAL_API_ENABLED:
    default: false
    requires_phronesis_ticket: true
  DF_153_ENV:
    default: dev

q0_sperr_liste:
  - id: Q0-1
    rule: "Inter-Entity-Transfer-Ausloesung"
    enforcement: "KEINE Treasury-API"
  - id: Q0-2
    rule: "Decision-Empfehlungen"
    enforcement: "Decision-Keyword-RegEx"

rho_estimate:
  annual_eur: 22000
  reason: "Cross-Entity-Revenue-Visibility PVG (HeyLou+9dots+LexVance)"
  schwer_quantifizierbar: false

migration_provenance:
  source_welle: 25
  pre_skeleton_engine_loc: 200
  reference_pattern: df-108
  skeleton_created: 2026-05-13

trinity_compliance:
  options_presented_in_spec: true
  variants:
    - conservative: "Mock-Mode default"
    - aggressive: "Real-API Phase-2"
    - contrarian: "Sunset bei externem Holding-Tool"

crux_bindung:
  K_0: "PVG-Group-Revenue-Visibility"
  Q_0: "n/a"
  I_min: "K11-K16 + LC1-LC5"
  W_0: "Manuelle Cross-Entity-Aggregation eingespart"

# [CRUX-MK]

# --- K11-16 Board-Haertung (evidence-based, df_k_hardening) [CRUX-MK] ---
K11_cascade_containment: lose_coupling  # evidence: lose_coupling-Block (LC1-5 Failure-Isolation)
K15_entropy_budget: bounded  # evidence: Output/Quota-Cap deklariert
K16_concurrent_spawn_mutex: wrapper_mkdir  # evidence: scripts/*.sh atomarer mkdir-Lock (Spawn-Mutex)
K_hardening_gap: [distillation_resistance, independent_ground_truth, human_override_decay]  # echte Luecke -> Code noetig
K_hardening_level: declared  # 3/6 evidenz-belegt
K_hardening_ts: 2026-06-23T04:26:38.615264+00:00
